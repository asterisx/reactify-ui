import { commonStyles } from '../../common';

export const styles = {
  container: {
    width: '2em',
    height: '2em',
    display: 'inline-block',
  },
  getFontSizeStyle: commonStyles.get.font.size,
};
