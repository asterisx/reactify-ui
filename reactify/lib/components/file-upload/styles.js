import { commonStyles } from '../../common';

export const styles = {
  noDisplay: {
    display: 'none',
  },
  cursorPointer: {
    cursor: 'pointer',
  },

  getDisabledStyle: commonStyles.get.disabled.style,
};
