// TODO Typeahead with a loading spinner
/**
 * <Typeahead onChange={(...args) => console.log(...args)}>
        <Typeahead.Suggestion
          isSelectable={false}
          index={1}
          value="value 1"
        >
          <div>
            <Balls />
            Loading
          </div>
        </Typeahead.Suggestion>
      </Typeahead>
 */

import { ComingSoon, withHeader } from '../../../common';

export default withHeader(ComingSoon);
